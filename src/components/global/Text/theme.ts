import type { HeadingTheme, TextTheme } from '@themes/types';

export const Heading = {
	baseStyle: () => ({
		color: 'Dark'
	})
} as unknown as HeadingTheme;

export const Text = {
	baseStyle: () => ({
		color: 'Dark'
	})
} as unknown as TextTheme;
